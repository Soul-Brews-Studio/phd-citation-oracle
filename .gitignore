# Secrets — never commit
.env
.env.*
*.key
*.pem
# Oracle runtime
ψ/active/
ψ/memory/logs/
.awaken-state.json
# OS
.DS_Store

# maw plugin install dir + index data (regenerated via maw plugin install)
.maw/
node_modules/
ψ/lab/**/node_modules/
ψ/lab/**/data/

# codex team runtime — auth symlinks, session logs, sqlite state. NEVER commit.
.codex/
.codex-team/

# standalone-runner vector store (derived — rebuild with `citation index`)
.citation/

# wrangler dev build/state dir for the optional embed worker
.wrangler/
